apiVersion: v1
kind: Service
metadata:
  name: file-service
  namespace: blog-app
spec:
  ports:
    - port: 8083
      targetPort: 8083
      name: http
  selector:
    app: file-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: file-service
  namespace: blog-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: file-service
  template:
    metadata:
      labels:
        app: file-service
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: node
                    operator: In
                    values: [node2, node3]
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            - labelSelector:
                matchLabels:
                  app: file-service
              topologyKey: kubernetes.io/hostname
      containers:
        - name: file-service
          image: registry.cn-beijing.aliyuncs.com/your-namespace/file-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8083
          envFrom:
            - configMapRef:
                name: app-config
            - secretRef:
                name: app-secret
