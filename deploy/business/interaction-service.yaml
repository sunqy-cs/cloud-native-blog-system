apiVersion: v1
kind: Service
metadata:
  name: interaction-service
  namespace: blog-app
spec:
  ports:
    - port: 8085
      targetPort: 8085
      name: http
  selector:
    app: interaction-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: interaction-service
  namespace: blog-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: interaction-service
  template:
    metadata:
      labels:
        app: interaction-service
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: node
                    operator: In
                    values: [node2, node3]
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            - labelSelector:
                matchLabels:
                  app: interaction-service
              topologyKey: kubernetes.io/hostname
      containers:
        - name: interaction-service
          image: registry.cn-beijing.aliyuncs.com/your-namespace/interaction-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8085
          envFrom:
            - configMapRef:
                name: app-config
            - secretRef:
                name: app-secret
